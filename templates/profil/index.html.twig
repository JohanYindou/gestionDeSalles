{% extends 'base.html.twig' %}

{% block title %}Profil de {{ app.user.firstname}}{% endblock %}

{% block body %}
    <div class="container">
        <div class="card">
            <div class="card-header">
                {{ dump(app.user) }}
            </div>
        </div>
    </div>
    
{% endblock %}
